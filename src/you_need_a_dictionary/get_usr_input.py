"""
Module to get user input based on a given sentence and word; then preprocess the input for proper API formatting.
"""

def get_user_input(sentence: str, word: str) -> str:
    """
    Get user input with the given sentence and word.

    Args:
        sentence (str): The prompt to display to the user.
        word (str): The word related to the prompt.

    Returns:
        str: The user's input.
    """
    
def preprocess_user_input(user_input: str) -> str:
    """
    Preprocess the user input for proper API formatting.

    Args:
        user_input (str): The raw input from the user.

    Returns:
        str: The preprocessed user input.
    """